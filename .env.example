# --- Required for first run --------------------------------------------------
# Copy to .env, supply your Hoodi endpoints and funded L1 account, then run
#   ./compose up --fresh
# The CLI handles first-run bootstrapping (wipes existing artifacts, deploys both rollups,
# and leaves the stack running). Subsequent invocations can drop --fresh.
# The Hoodi chain ID (560048) is baked into the tooling—you never need to set it.

HOODI_EL_RPC=https://hoodi.example.org/el
HOODI_CL_RPC=https://hoodi.example.org/cl
WALLET_PRIVATE_KEY=0xinsert_funded_l1_private_key
WALLET_ADDRESS=0xinsert_funded_l1_address

# --- Optional shared publisher + helper contracts ---------------------------
# The shared publisher reuses HOODI_EL_RPC for its L1 RPC endpoint by default.
SP_L1_SUPERBLOCK_CONTRACT=0x0000000000000000000000000000000000000001
# SP_L1_SHARED_PUBLISHER_PK_HEX=0xfcd75a5bc3e7e37c9453e99bb30500d91d79bcea3ad3af944805a38e511df1e6
# SP_L1_FROM_ADDRESS=0x254a2B6Dc3441CA0ED69513dC5531F3FB3b64036
# (Update both when you swap to a different wallet; defaults reuse the funded key above.)
SP_L1_DISPUTE_GAME_FACTORY=0x0000000000000000000000000000000000000001
# Uncomment to override the default L2 chain IDs (77777 and 88888).
# ROLLUP_A_CHAIN_ID=77777
# ROLLUP_B_CHAIN_ID=88888

# The bundle defaults to ./contracts; override SOURCE if needed.
CONTRACTS_SOURCE=./contracts

# Point these at existing source checkouts to skip cloning and build directly
# from local changes. Leave commented to reuse the copies under this repo.
# OP_GETH_PATH=/absolute/path/to/op-geth
# ROLLUP_SHARED_PUBLISHER_PATH=/absolute/path/to/rollup-shared-publisher

# --- Advanced knobs (uncomment as needed) -----------------------------------
# OPTIMISM_REPO=https://github.com/ethereum-optimism/optimism.git
# OPTIMISM_REF=op-node/v1.13.4
# OP_GETH_REPO=https://github.com/ssvlabs/op-geth.git
# OP_GETH_BRANCH=stage
# ROLLUP_SHARED_PUBLISHER_REPO=git@github.com:ssvlabs/rollup-shared-publisher.git
# ROLLUP_SHARED_PUBLISHER_BRANCH=stage
# ROLLUP_SHARED_PUBLISHER_PATH=/absolute/path/to/rollup-shared-publisher
# DEPLOYMENT_TARGET=live
# DEPLOY_CONTRACTS=1
# CONTRACT_DEPLOY_DELAY_SECONDS=10
# CONTRACT_DEPLOY_RETRY_DELAY_SECONDS=5
# CONTRACT_DEPLOY_MAX_ATTEMPTS=5
# ROLLUP_ACCOUNT_GENESIS_BALANCE_WEI=100000000000000000
# ROLLUP_ACCOUNT_MIN_BALANCE_WEI=1000000000000000000
# ROLLUP_ACCOUNT_DEPOSIT_WEI=0
# ROLLUP_DEPOSIT_GAS_LIMIT=200000
# ROLLUP_DEPOSIT_WAIT_ATTEMPTS=120
# ROLLUP_DEPOSIT_WAIT_DELAY=5
# ROLLUP_PRAGUE_TIMESTAMP=0
# ROLLUP_ISTHMUS_TIMESTAMP=0
# RPC_WAIT_ATTEMPTS=10
# RPC_WAIT_DELAY=3
